<!-- 
	@file GameProject/OpenProjectView.xaml
	@author LinhengXilan
	@version 0.0.0.12
	@date 2026-5-6
-->
	
<UserControl x:Class="Editor.GameProject.OpenProjectView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:Editor.GameProject"
			 mc:Ignorable="d" Width="800" Height="500"
			 >
	<UserControl.DataContext>
		<local:OpenProjectModel/>
	</UserControl.DataContext>
	<StackPanel Background="#1F1F1F" Orientation="Vertical">
		<ListBox Width="600" Height="359" ItemsSource="{Binding ProjectInfos}" x:Name="projectInfosListBox"
		         SelectedIndex="0">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal" Height="35">
						<Image Source="{Binding ProjectIconPath}" Width="32" Height="32" Margin="0,0,4,0"/>
						<StackPanel Orientation="Vertical" VerticalAlignment="Center">
							<TextBlock Height="15" Text="{Binding ProjectName}"/>
							<TextBlock Height="15" Text="{Binding ProjectPath}"/>
						</StackPanel>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0, 30, 0, 21">
			<Button Content="返回" Width="60" Height="20" IsCancel="True"/>
			<Button Content="打开" Width="60" Height="20" Margin="30, 0, 0, 0" IsDefault="True"/>
		</StackPanel>
	</StackPanel>
</UserControl>
