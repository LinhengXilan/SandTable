<!--
	@file ProjectBrowser/ProjectBrowserWindow.xaml
	@author LinhengXilan
    @version 0.0.0.22
    @date 2025-6-3
-->

<Window x:Class="Editor.ProjectBrowser.ProjectBrowserWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:Editor.ProjectBrowser"
		xmlns:loadProject="clr-namespace:Editor.ProjectBrowser.LoadProject"
		xmlns:newProject="clr-namespace:Editor.ProjectBrowser.NewProject"
		mc:Ignorable="d"
		Title="ProjectBrowserWindow"
		WindowStyle="None" AllowsTransparency="True"
		MinWidth="640" MinHeight="480">

	<WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"/>
    </WindowChrome.WindowChrome>

	<Window.DataContext>
		<local:ProjectBrowserViewModel/>
	</Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#1C1C1C"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#1C1C1C"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#1C1C1C"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#1C1C1C"/>
        </Style>
    </Window.Resources>

	<Grid Background="#2C2C2C">
		<Grid.RowDefinitions>
			<RowDefinition Height="30"/>
			<RowDefinition />
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0"
                    Orientation="Horizontal" HorizontalAlignment="Right"
                    WindowChrome.IsHitTestVisibleInChrome="True">
			<Button Width="40" Background="#00FF00" Click="MinimizationButtonClicked" />
			<Button Width="40" Background="#FFFF88" Click="MaximizationButtonClicked"/>
			<Button Width="40" Background="#FF0000" Click="CloseButtonClicked"/>
		</StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type loadProject:LoadProjectViewModel}">
                    <loadProject:LoadProjectView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type newProject:NewProjectViewModel}">
                    <newProject:NewProjectView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
	</Grid>
</Window>